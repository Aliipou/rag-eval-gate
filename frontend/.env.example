# Copy to .env.local for local dev. Points the frontend at the FastAPI
# backend (run separately: `uvicorn api.main:app --reload` from the repo
# root). No hosted deployment of either side exists for this project --
# see README "What is implemented / what is not".
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
